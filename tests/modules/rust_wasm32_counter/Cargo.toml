[package]
name = "rust-wasm32-counter"
version = "0.1.0"
edition = "2018"

[lib]
crate-type = ["cdylib"]

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0.87"
atomic-counter = "1.0.1"
once_cell = "1.16.0"
lens_sdk = { path = "../../../sdk-rust" }
